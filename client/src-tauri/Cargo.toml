[package]
name = "client"
version = "0.1.0"
description = "Local-first AI agents with an isolated Linux computer"
authors = ["OpenAgentFleet"]
edition = "2021"
license = "Apache-2.0"
homepage = "https://openagentfleet.xyz"
repository = "https://github.com/robbyczgw-cla/openagentfleet"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "client_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
getrandom = "0.3"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(target_os = "macos")'.dependencies]
block2 = { version = "0.6.2", default-features = false, features = ["alloc"] }
objc2 = "0.6.4"
objc2-app-kit = { version = "0.3.2", features = [
  "NSAlert",
  "NSApplication",
  "NSButton",
  "NSControl",
  "NSResponder",
  "NSSecureTextField",
  "NSTextField",
  "NSView",
] }
objc2-foundation = { version = "0.3.2", features = ["NSGeometry", "NSString"] }
objc2-avf-audio = "0.3.2"
objc2-speech = "0.3.2"
zeroize = "1"
